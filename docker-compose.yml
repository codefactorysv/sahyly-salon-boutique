services:
  app:
    build: .
    container_name: sahylysalon-app
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "127.0.0.1:8090:3000"
    volumes:
      - ./data:/app/data
